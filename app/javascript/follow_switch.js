function follow_switch() {
  const followingSwitch = document.getElementById("following_switch")
  const followerSwitch = document.getElementById("follower_switch")

  if (!followingSwitch || !followerSwitch) return null
  
}


window.addEventListener("turbo:load", follow_switch)