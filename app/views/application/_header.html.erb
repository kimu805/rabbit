<% if flash[:notice] %>
  <div id="flash-message" class="flash-message <%= flash[:notice] ? 'notice' : flash[:alert] ? 'alert' : '' %>">
    <span class="icon">
      &#x2714; <%= flash[:notice] %> <!-- チェックマークアイコン -->
    </span>
  </div>
<% elsif flash[:alert] %>
  <div id="flash-message" class="flash-message <%= flash[:notice] ? 'notice' : flash[:alert] ? 'alert' : '' %>">
    <span class="icon">
      &#x26A0; <%= flash[:alert] %> <!-- 警告アイコン -->
    </span>
  </div>
<% end %>

<header class= "header_box">
  <div class= "header_left_box">
    <div class= "header_left_appTitle">Rabbit</div>
  </div>
  <div class= "header_right_box">
    <nav class= "header_navigation_box">
      <div class= "header_navigation_toggle">
        <div class= "bar"></div>
        <div class= "bar"></div>
        <div class= "bar"></div>
      </div>
      <ul class= "header_navigation_menuList">
        <li class= "header_navigation_heading1">ユーザー</li>
        <% if user_signed_in? %>
          <li><%= link_to "マイページ", user_path(current_user), class: "link header_navigation_heading2" %></li>
          <li><%= link_to "ログアウト", destroy_user_session_path, data: {turbo_method: :delete}, class: "link header_navigation_heading2" %></li>
        <% else %>
          <li><%= link_to "新規登録", new_user_registration_path, class: "link header_navigation_heading2" %></li>
          <li><%= link_to "ログイン", new_user_session_path, class: "link header_navigation_heading2" %></li>
        <% end %>
        <li><%= link_to "タグ", tags_path, class: "link header_navigation_heading1" %></li>
        <% Tag.all.each do |tag| %>
          <li><%= link_to tag.name, tag_path(tag), class: "link header_navigation_heading2" %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>