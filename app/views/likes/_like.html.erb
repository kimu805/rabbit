<%= turbo_frame_tag project do %>
  <% if user_signed_in?%>
    <% if project.liked_by?(current_user) %>
      <%= link_to(project_likes_path(project), data: {turbo_method: :delete}, class: "link", style: "color: black;") do %>
        <div class= "project_like">
          💖 <%= project.likes.count %>
        </div>
      <% end %>
    <% else %>
      <%= link_to(project_likes_path(project), data: {turbo_method: :post}, class: "link", style: "color: black;") do %>
        <div class= "project_like">
          ❤️ <%= project.likes.count %>
        </div>
      <% end %>
    <% end %>
  <% else %>
    <div class= "project_like">
      ❤️ <%= project.likes.count %>
    </div>
  <% end %>
<% end %>